# NEXT.md — the queue

_Rewritten 2026-09-23 (D-970 audit: the prior version said 18 clocks/30 guards and predated
the deployment runbook — live: 34 clocks, 32 guards, deployed 5-sleeve candidate)._

## Front of queue (operator-keyed — the engine has pre-staged each)

1. NI number → IBKR UK margin application (docs/BROKER_SETUP.md) → gateway allowlist line →
   1-share adapter verification with the kill-switch proven to refuse.
2. Seed ballast + wealth ledger (3 rows, minutes) — named the program's real failure at
   D-823 and still empty.
3. Two allowlist lines (`data.binance.vision`, Solana/pump.fun set) + free TRACE credential.
4. First hand fill on the micro rung (`MICRO_BUDGET` + `micro-ledger.ts`).
Full register: the 14-row bottleneck table in the 2026-09-23 ecosystem audit.

## Engine queue (registered or law-bound; research otherwise FROZEN, D-823)

1. Execute `D-970-crowd-dc-adoption` re-measure (deadline 2026-10-07, rule is numeric and
   two-sided in trd_prereg).
2. Score `D-970-gex-sizing-calibration` when the D-969 per-strike file reaches 40 shared
   days (then every +20).
3. XMR Asian-session real-spread measurement (free; decides `fwd-xmr-asia-rangefade`'s
   25bp cost assumption — audit underexploited #1).
4. Restore-block follow-ups: decisions-guard ID-continuity rule (shipped with the D-918–956
   restoration); STATE-staleness check into the docs cycle.
5. Pre-register the estimate-revisions PEAD question BEFORE the data matures (audit #6);
   register or sunset the Blockchair consumer question (audit #7).
6. Underexploited joins ledger (audit §3, 12 rows): SI×IVOL-leg, 13F×short-family,
   OI×cryptomom de-gross, BLS×sizing — each needs mechanism registration first.

## Standing

- Clocks age toward their own verdicts; the scorers and continuity guard own them.
- Every new claim from outside lands in assay first (18 rows).
- The commonwealth mission spec has been RELOCATED OUT of this repo (this one is public) to
  the private command-centre repo, which is also where that mission executes. Aegis's role in
  it is the treasury/endowment only.
