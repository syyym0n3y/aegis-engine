# METHODOLOGY FLAWS — the complete register, including what nothing guards (D-836, 2026-09-08)

> The operator asked to understand every flaw in our methodology across the board. This is that, written to be
> uncomfortable rather than reassuring. **Section A** is what we found and guarded. **Section B** is what we found and
> did NOT guard. **Section C** is what nobody has hit yet but which I can identify by reasoning — the most valuable
> section, and the one with no evidence behind it, which is stated rather than hidden.
>
> The programme's own history says the ratio matters: 30 laws exist, every one written after a defect had already
> reached a conclusion. Section C is an attempt to get ahead of that for once.

## A. Found, and mechanically guarded (30 laws, 30 guards, board green)

Coverage · liquidity · effect-size · breadth · execution (+ same-bar) · instrument · pre-commitment · holdability ·
sign · universe (+ coverage-is-not-breadth) · selection · mechanism · precondition · continuity · benchmark ·
turnover · cost-inflation · deflation-paid · agent-output · plumbing · daemon-drift · schema-honesty · market-cap
share-base · sovereignty · permissions · decisions-mechanism · REST-restart · trial-idempotency · registry · gap-register.

Each is enumerated with its origin in `CLAUDE.md`. The property that matters is that **each was verified by being made
to go RED**, not merely by passing.

## B. FOUND AND NOT GUARDED — live exposure, today's date

| # | flaw | how it was found | why no guard yet | exposure |
|---|---|---|---|---|
| B1 | **Benchmark measured over a different WINDOW than the events it judges** | D-826b: retracted the programme's first SUPPORTED result the same day; D-826c found a second instance in `seasonality.ts` | the benchmark guard reads the LEDGER text and can see that a universe mean is STATED, never which window produced it. Detecting it needs code analysis, not text | **HIGH** — it flipped a verdict once already, and reverses signs rather than shrinking them |
| B2 | **t-statistics on OVERLAPPING windows** | D-416 named it; D-830 measured a **ten-fold** inflation (t 14.99 overlapping vs 1.57 honest) on the same regression | no mechanical check that a reported t used non-overlapping sampling | **HIGH** — a 10x inflation is enough to manufacture any result |
| B3 | **A monitor or regex reading the WRONG number** | D-833: a retest-harness pick matched the TRAIN row instead of the TEST row; it would never have fired | nothing verifies a pick against live output | MEDIUM — silent, and the board stays green |
| B4 | **A multi-part write failing halfway** | D-833: a script died mid-edit, so a commit shipped a change WITHOUT the entry documenting it — and the decisions guard passed because the entry did not exist to be checked | a guard cannot flag an absence it was never told to expect | MEDIUM |
| B5 | **Our own tooling's parse/vocabulary errors** | subheadings `### D-828` parsed as separate entries; `GOLD: gate` rejected because the vocabulary is fixed (twice: D-823, D-832) | these DO go red — but as false splits and false reds, which train the reader to dismiss the guard | LOW-MEDIUM |

## C. IDENTIFIED BY REASONING, NOT YET HIT — no evidence, stated as speculation

**These are the ones worth the operator's attention, because every entry in section A was once in this section and
nobody wrote it down.**

> **STATUS, 2026-09-10 — the section is no longer speculation.** All nine entries have now been tested (D-842 through
> D-848). Six were CONFIRMED, one in a different shape than it was written and with its own stated evidence refuted
> (C6); one is NOT TESTABLE as the record stands and the fix is metadata (C7); C4 is closed. **C9 did not exist when
> this section was written — it was found by C8's positive control, which is the whole argument for insisting on one.**
> Two of the tests failed their own controls first (D-846, D-844) and were re-registered rather than re-cut. The
> section is kept open deliberately: the value was never in these nine, it was in the habit of writing down what
> nobody has hit yet.

| # | suspected flaw | the argument | how to test it cheaply | if true, what it invalidates |
|---|---|---|---|---|
| C1 | **TESTED (D-846/846b) — CONFIRMED, and larger than the argument guessed: the price we store for a 1980s session is 96% away from the price that traded.** Adjusted price LEVELS are not what traded | our equity bars are fully adjusted. Adjusted RETURNS are correct, but a rule keyed to a LEVEL references a price that never existed | **DONE: `scripts/adjusted-levels.ts`.** Nominal price RECONSTRUCTED, not assumed: adjustment scales price and price-increment equally, so f = measured tick / regulated tick (all three tick regimes measured in D-843). D-846 FAILED its own lattice control at 61.6% pooled and the failure decomposed by regime — 82.7% eighths, 63.6% sixteenths, 42.4% decimals — so the working scope was RE-REGISTERED as D-846b rather than carved out after the fact | Median stored vs traded: $0.72 vs $24.76 (1970s), $1.95 vs $30.50 (1980s), $5.04 vs $34.87 (1990s). A rule firing within 5c of a whole dollar fires 1.61x more often in nominal space. **Adjusted RETURNS remain correct and every return-based result is untouched.** Only ABSOLUTE-level rules on the equity panel are invalid; RELATIVE level rules are largely safe except across an adjustment event, which stays UNTESTED, as does the whole decimal era |
| C2 | **TESTED (D-842), RE-TESTED ON THE HONEST ESTIMATOR (D-845) — CONFIRMED IN PART and UNCHANGED by the re-test.** The UTC day boundary is an arbitrary choice the results depend on | D-828 found crypto extremes cluster 00:00-06:00 UTC. A "day" for a 24-hour instrument is a convention, and every prior-session level, daily range and day-clustered statistic inherits it | **DONE: `scripts/boundary-robustness.ts`.** Range predictability HOLDS at every boundary. Boundary spread 0.093 against the 0.009 train-test drift D-828 cited as stability. **D-845 re-ran it on the per-instrument estimator after D-844b showed the pooled one was inflated: spread falls to 0.058, but RELATIVE to each estimator own level the two are nearly identical (15.2% vs 15.7%)** | not the finding, but the PRECISION claimed for it. The conclusion is unchanged in substance: the convention moves the statistic by the same fraction of itself on either estimator; only the number of correlation points shrank, with the level |
| C3 | **TESTED (D-843) — CONFIRMED, and larger than the argument guessed: cost fell 19x, and the flat assumption manufactures a decay story.** Costs are treated as constant across decades | D-830 spans 1990-2026 and D-655 spans 1963-2026 at one cost assumption. Fixed commissions ran to May 1975, eighths to June 1997, sixteenths to April 2001 | **DONE: `scripts/era-cost.ts`.** The minimum tick read off the price lattice (scale-free, so flaw C1 cannot bite it; a hard LOWER BOUND on cost): **51.8bp in the 1970s falling to 2.7bp in 2015+, 19.2x**, with the 1997 sixteenths step (-47.6% against the ~50% the tick change predicts) and decimalisation (-72.7%) both recovered as positive controls. Corwin-Schultz, the standard estimator, **could not see this at all** (0.94x) and is reported as a failed instrument rather than deleted | **NULL on the pre-registered rule** (pre-1990 net +0.79%/yr, the rule required <=0) but the measurement stands: D-655 reads 2.69 -> 0.46%/yr at flat cost (a decay) and **0.79 -> 1.80%/yr at each era own cost (an improvement)**. Any "the anomaly decayed" reading of a long-span GROSS series is now suspect, because the cost of harvesting fell faster than the premia did. **One result was re-costed; every other long-span cost-charged row on the board still carries the flat-cost flaw** |
| C4 | **TESTED, CONFIRMED, AND NOW CLOSED (D-836 → D-841): 3 real gaps found and fixed; 2 of the reported 5 were the detector, not the guards.** Guards are verified once, at birth, and never re-verified | four guards have been found broken AFTER shipping (D-584 fail-open, D-650, D-659 silent, D-671). `registry-guard` checks a guard EXISTS; nothing checks it can still go RED | **DONE and CLOSED: `scripts/guard-selftest-all.ts`, in the daily runner. 30 of 30 now demonstrate a working red branch.** The first report of 5 exposed was wrong in a way worth keeping: `coverage-guard` DID refuse (exit 1 on an injected impossible floor) but never printed that it was self-testing, and `market-cap-guard` prints "SELFTEST 1 PASSED" which the detector's regex could not match — **2 of the 5 were the detector**. The 3 real gaps (`continuity`, `gap-register`, `infra`) had no self-test at all and now each inject a subject that MUST be refused: an absent feed + an unregistered job; a gap marked FILLED with a backing that cannot exist; a probe of a dead port | the board's green, which is the single most load-bearing claim in the programme — now measured rather than assumed |
| C5 | **TESTED (D-847) — CONFIRMED IN PART, and better than assumed: 84% cite a decision. But TRACEABLE IS NOT JUSTIFIED.** Every guard THRESHOLD is itself an unregistered researcher choice | t >= 2.0, 60% instrument agreement, 1.5x universe spread, 50-name breadth floor — each picked once and never justified against an alternative | **DONE: `scripts/threshold-guard.ts`, on the board.** 25 deciding thresholds across 31 guards, 21 traceable, 4 not. Each load-bearing one traces only to the entry that COINED it: the 50-name floor is a round number above the 14 at which D-443 actually collapsed, and the 1.5x bar was set BELOW the 2.1x D-535 had just measured — fitted to the observation that created it. Over 801 stated t-statistics, **19.0% sit in the 2.0-3.0 band where moving the convention changes the answer** | not results directly but the appearance of rigour. **The asymmetry: promotion gates are insert-only and cite a decision; guard thresholds are registered nowhere.** Shipped as a RATCHET (freeze at 4, red on a new one, only ever down) because a guard demanding retroactive cleanup gets switched off |
| C6 | **TESTED (D-848) — CONFIRMED IN A DIFFERENT SHAPE, and its own stated evidence REFUTED.** The hypothesis set is supplied by the operator and by me, and both are biased | 2.9M trials, but the SPACE searched is what one person suggested and one model thought of | **DONE: `scripts/hypothesis-provenance.ts`.** 284 rows by stated origin: **follow-on from a previous entry of this programme 64.1%**, literature 11.6%, enumeration 5.6%, self-attack 2.5%, accident 1.8%, **operator 1.4%** | The search is not shaped by the operator and is barely systematic: it is SELF-REFERENTIAL, and a search that mostly asks what its last answer suggested explores a neighbourhood rather than a space. **The register argued survivors were found ACCIDENTALLY and that is wrong: accidental rows survive at 0.54x the base rate, BELOW average, while self-attack survives at 1.55x and enumeration at 1.35x — the two most deliberate origins yield most.** What cannot be measured is the questions never asked, which is the real claim |
| C7 | **TESTED (D-848) — NOT TESTABLE AS THE RECORD STANDS, and that is the finding.** Post-publication decay is applied inconsistently | D-500 found the announcement premium decayed post-publication; that reasoning is applied to some literature signals and not others | **DONE, inside D-848: only 5 of 33 literature-derived rows (15%) carry a publication year distinguishable from a data-span date.** The fix is metadata, not analysis | the interpretation of every null on a published anomaly. **D-843 sharpened it from the cost side: every "it decayed" reading is a GROSS-return story told against a cost curve that fell 19x, and on the one case measured the net ROSE.** |
| C8 | **TESTED (D-844) — CONFIRMED for the panel that matters, and on the evidence it does not move the statistic.** Survivorship in the DATA SOURCE, not just the universe | Yahoo and the CFD feed serve what exists today. D-639/645 handled the equity delisting hole; nothing checked the intraday panels | **DONE: `scripts/panel-survivorship.ts`.** Liveness read from the venue's own status list (897 listed, 766 TRADING). **tf=1h holds 0 dead of 25; tf=1dSF, built survivor-free for exactly this reason, holds 138 of 512 (27.0%)** — the repair was done on the daily panel and never propagated to the hourly one. Depth-matched per-contract alive-vs-dead gap in corr(range, prior range): **0.014** against the 0.093 floor | **UNTESTED on the registered rule** (the positive control failed and the threshold was not moved), with half (a) standing as a measurement. The universe STATEMENT on D-827/828/829/842 needs narrowing, not the finding. **The FX/index CFD series has no venue status list to check against and remains genuinely UNTESTED.** And the control's failure produced the larger result — see C9 |
| C9 | **NEW, FOUND BY C8's CONTROL (D-844b) — CONFIRMED: a headline pooled across heterogeneous instruments manufactures its own magnitude.** | D-828's 0.60 range autocorrelation is POOLED across 24 instruments whose volatility levels differ by an order of magnitude; a high-volatility instrument has a large range on both days, so pooling creates correlation that no constituent has | **DONE, inside the same script.** D-828's exact recipe on its own 24 instruments: **pooled 0.599 vs per-instrument median 0.378**, an inflation of **0.221 = 2.4x** D-842's precision floor; by class 0.498 crypto / 0.710 FX-index. Bar source verified irrelevant (0.497 vs 0.497), window verified near-irrelevant (0.497 vs 0.499) | The MAGNITUDE of every pooled cross-instrument statistic on this board, and the confidence built on it. The finding survives at 0.378; the number 0.60 does not. **D-842 was a self-attack on this same statistic and inherited the same pooling** — a self-attack that shares its target's defect tests the assumption it should be questioning, and D-842's 0.093 boundary spread is itself a pooled quantity |

## D. The one flaw that is not methodological

Ten weeks, 2.9M trials, 276 catalogued tests, 30 laws, 19 clocks, **zero trades and an empty ledger**. Every flaw above
is a reason a *finding* might be wrong. None of them is why there is no money. The binding constraint is that the
capital and the account do not exist yet, and no methodological improvement changes that — which is why D-823 froze
research behind an ACTS-ON requirement and D-679 recorded that the research was aimed at a capital regime the account
is not in.

## E. On 10^7

The operator's standard is a 10^7 effect. Stated honestly against this record: **no fix in sections A–C produces it.**
Better methodology makes the nulls more reliable; it does not create a return. The three things on this record with any
claim to that order of magnitude are (1) the structural lane — deposits × compounding × wrapper, certain but linear in
capital and slow; (2) the capacity-inverted mechanisms of D-834, which are real, small, and unverified for UK access — **now with one measured per-head number: the reachable venue sign-up set is ~£150–£400 once (D-851), 5–15% of the account, non-compounding**;
and (3) something not yet in the search space, which section C6 argues is the most likely place it lives. **A
10^7 outcome is far more likely to come from widening what we ask than from tightening how we test.** That is the
uncomfortable conclusion of a register of methodology flaws, and it is the honest one.
